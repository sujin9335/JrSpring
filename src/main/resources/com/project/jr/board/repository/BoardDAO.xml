<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.jr.board.repository.BoardDAO"> 
	
	<!-- 게시판 목록보기 > 게시글 정보 가져오기 -->
	<select id="list" parameterType="com.project.jr.board.model.PageDTO" resultType="com.project.jr.board.model.BoardDTO">
	SELECT
		*
	FROM (SELECT 
				b.*, rownum AS rnum 
			FROM (SELECT 
					* 
					FROM vwBoard 
					WHERE isBoardShow = 1 AND boardType = 1
					<if test="search != null">
					    AND ${column} LIKE '%' || #{word} || '%'
					</if>
					ORDER BY boardSeq DESC) b)
	WHERE rnum BETWEEN #{begin} AND #{end}
	</select>
	
	<!-- 게시판 목록보기 > 총 글의 개수 -->
	<select id="getTotalCount" parameterType="com.project.jr.board.model.PageDTO" resultType="Integer">
		SELECT count(*) AS cnt FROM tblBoard 
		WHERE isBoardShow = 1 AND boardType = 1
		<if test="search != null">
		    AND ${column} LIKE '%' || #{word} || '%'
		</if>
	</select>
	
	<!-- 게시판 새 글 작성 -->
	<insert id="add" parameterType="com.project.jr.board.model.BoardDTO">
		INSERT INTO tblBoard (boardSeq, boardTitle, boardContent,id, boardWriteDate, boardLike, boardReport, boardHits, boardType, isBoardShow)
				VALUES (((SELECT max(boardSeq) FROM tblBoard) + 1), #{boardTitle}, #{boardContent}, #{id}, sysdate, 0, 0, 0, 1, 1)
	</insert>
	
	<!-- 게시판 상세보기 > 글 상세정보 가져오기 -->
	<select id="get" parameterType="String" resultType="com.project.jr.board.model.BoardDTO">
		SELECT 
			b.*,
			(SELECT count(*) FROM tblComment WHERE boardSeq = b.boardSeq) AS ccnt
		FROM tblBoard b 
		WHERE boardSeq = #{boardSeq}
	</select>
	
	<!-- 게시판 상세보기 > 조회수 카운팅 -->
	<update id="updateReadcount" parameterType="String" >
		UPDATE tblBoard SET boardHits = boardHits + 1 WHERE boardSeq = #{boardSeq}
	</update>
	
	<!-- 게시글 삭제 -->
	<update id="del" parameterType="String">
		UPDATE tblBoard SET isBoardShow = 0 WHERE boardSeq = #{boardSeq}
	</update>
	
	<!-- 게시글 수정 -->
	<update id="edit" parameterType="com.project.jr.board.model.BoardDTO">
		UPDATE tblBoard SET boardTitle = #{boardTitle}, boardContent = #{boardContent} WHERE boardSeq = #{boardSeq}
	</update>
	
	<!-- 게시글 좋아요 -->
	<update id="like" parameterType="String">
		UPDATE tblBoard SET boardLike = boardLike + 1 WHERE boardSeq = #{boardSeq}
	</update>
	
	<!-- 게시글 좋아요 취소 -->
	<update id="unlike" parameterType="String">
		UPDATE tblBoard SET boardLike = boardLike - 1 WHERE boardSeq = #{boardSeq}
	</update>

</mapper>